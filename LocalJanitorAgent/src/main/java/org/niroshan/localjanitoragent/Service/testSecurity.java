package org.niroshan.localjanitoragent.Service;


public class testSecurity {
/*
    public static void main(String[] args) {
        SafetyService safetyService = new SafetyService();
        if(args.length == 0){
            System.out.println("Nor arg provided");
        }else{
            System.out.println("Safetry service is "+ safetyService.isSafe(args[0]));
        }
        //System.out.println("safetyService is " + safetyService.isSafe(args[0]));
        System.out.println("Is 'my_pic.jpg' safe? " + safetyService.isSafe("my_pic.jpg")); // Should be TRUE
        //System.out.println("Is '../WINDOWS/system32' safe? " + safetyService.isSafe("../WINDOWS/system32"));
        System.out.println("Is '../WINDOWS/system32' safe? " + safetyService.isSafe("../D"));

    }
*/
}
